<h1> Liste des salariés</h1>

<div class="container d-flex flex-row flex-wrap gap-3 ">

    <% employees.forEach(user => { %>
        <div class="card d-flex flex-column justify-content-between align-items-center col-3">
            <div>
                <img src="<% user.avatar %>" alt="photo de profil" class="img-fluid">
                <h3>
                    <%= user.lname %> <%= user.fname %>
                </h3>
            </div>
            <div>
                <p>
                    <strong>Poste:</strong>
                </p>
                <div>
                    <button><a href="">Voir Plus</a></button>
                </div>
            </div>

        </div>
    <% }) %>
</div>

<div>
    <a href="/ajout-salarie"><button>+</button></a>
</div>